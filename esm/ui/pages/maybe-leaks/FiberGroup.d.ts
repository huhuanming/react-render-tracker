import * as React from "react";
import { MessageFiber } from "../../types";
export declare function FiberGroup({ typeId, fibers, initExpanded, }: {
    typeId: number;
    fibers: MessageFiber[];
    initExpanded?: boolean;
}): React.JSX.Element;
