import * as React from "react";
import { TransferChangeDiff, ValueTransition } from "../../../types";
export declare function Diff({ diff, values, }: {
    diff?: TransferChangeDiff;
    values: ValueTransition;
}): React.JSX.Element;
